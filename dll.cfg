MEMORY {
  DLL_LOAD_ADDR:  start = $7000, size = $1000, file = %O;
}

SEGMENTS {
  DLL_API: load = DLL_LOAD_ADDR, type = ro;
  DATA:    load = DLL_LOAD_ADDR, type = ro;
  CODE:    load = DLL_LOAD_ADDR, type = ro;
  RODATA:  load = DLL_LOAD_ADDR, type = ro;
}

